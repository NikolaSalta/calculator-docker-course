# 📖 Словарь терминов Docker

## Базовые термины Docker

### Docker
**Что это:** Программа для контейнеризации приложений.

**Простыми словами:** Docker позволяет "упаковать" приложение вместе со всем необходимым (библиотеки, настройки, зависимости) в один "контейнер". Этот контейнер можно запустить на любом компьютере, и он будет работать одинаково.

**Аналогия:** Контейнер для переезда — ты складываешь все вещи в коробку, и неважно куда переезжаешь, всё останется на месте и будет работать.

---

### Image (Образ)
**Что это:** Неизменяемый шаблон для создания контейнеров.

**Простыми словами:** Образ — это как рецепт или чертёж. Сам по себе он ничего не делает, но по нему можно создать работающее приложение (контейнер). Образ содержит операционную систему, приложение и все его зависимости.

**Аналогия:** Рецепт торта — сам рецепт это не торт, но по нему можно испечь сколько угодно тортов.

**Команды:**
```bash
docker images              # Список образов
docker build -t name:tag . # Создать образ
docker rmi name:tag        # Удалить образ
docker pull name:tag       # Скачать образ
```

---

### Container (Контейнер)
**Что это:** Запущенный экземпляр образа.

**Простыми словами:** Контейнер — это реально работающее приложение, созданное по "рецепту" (образу). Можно создать много контейнеров из одного образа. Каждый контейнер изолирован от других.

**Аналогия:** Испечённый торт — конкретный торт, сделанный по рецепту.

**Команды:**
```bash
docker ps                    # Список запущенных
docker ps -a                 # Список всех (включая остановленные)
docker run image             # Создать и запустить
docker stop container        # Остановить
docker rm container          # Удалить
```

---

### Dockerfile
**Что это:** Текстовый файл с инструкциями для создания образа.

**Простыми словами:** Dockerfile — это пошаговый рецепт. Docker читает его строка за строкой и выполняет команды: "возьми базовый образ", "скопируй файлы", "установи зависимости", "запусти приложение".

**Аналогия:** Пошаговый рецепт: "возьми муку, добавь яйца, перемешай, поставь в духовку".

**Пример:**
```dockerfile
FROM python:3.12        # Базовый образ
WORKDIR /app            # Рабочая директория
COPY . .                # Копировать файлы
RUN pip install -r requirements.txt  # Установить зависимости
CMD ["python", "app.py"]  # Команда запуска
```

---

### Docker Daemon
**Что это:** Фоновая служба, которая управляет Docker.

**Простыми словами:** Docker Daemon — это "повар на кухне". Он всегда должен работать, чтобы выполнять команды: создавать контейнеры, управлять сетями, хранить образы.

**Если не работает:**
```
Cannot connect to the Docker daemon
```
Решение: Запустить Docker Desktop (Mac/Windows) или `sudo systemctl start docker` (Linux).

---

### Docker Hub
**Что это:** Онлайн-хранилище образов Docker.

**Простыми словами:** Это как "магазин" готовых образов. Можно скачать образ Python, Node.js, nginx, PostgreSQL и других программ вместо того, чтобы создавать с нуля.

**Аналогия:** App Store для Docker-образов.

**Команды:**
```bash
docker pull nginx           # Скачать образ
docker push myimage:tag     # Загрузить свой образ
```

---

## Термины сети

### Port (Порт)
**Что это:** Числовой адрес для сетевого соединения.

**Простыми словами:** Порт — это как "номер квартиры" внутри компьютера. IP-адрес — это адрес дома, а порт — номер квартиры. Чтобы доставить данные, нужно знать и то, и другое.

**Стандартные порты:**
- 80 — HTTP (веб-сайты)
- 443 — HTTPS (безопасный веб)
- 22 — SSH
- 3000 — часто используется для разработки
- 8080 — альтернативный HTTP

---

### Port Mapping (Проброс портов)
**Что это:** Связывание порта хоста с портом контейнера.

**Простыми словами:** Контейнер изолирован — к нему нельзя подключиться напрямую. Проброс портов создаёт "мост" между портом на твоём компьютере и портом внутри контейнера.

**Формат:** `-p ПОРТ_ХОСТА:ПОРТ_КОНТЕЙНЕРА`

**Пример:**
```bash
docker run -p 3000:80 nginx
```
- Запрос на localhost:3000 (твой компьютер)
- Перенаправляется на порт 80 внутри контейнера

---

### Host (Хост)
**Что это:** Твой физический компьютер, на котором работает Docker.

**Простыми словами:** Хост — это "здание", в котором находятся все контейнеры. Это твой Mac, Windows или Linux компьютер.

---

### Docker Network (Сеть Docker)
**Что это:** Виртуальная сеть для связи контейнеров.

**Простыми словами:** Контейнеры по умолчанию изолированы. Docker Network создаёт виртуальную сеть, где контейнеры могут "видеть" друг друга по именам.

**Аналогия:** Внутренний телефон в офисе — можно позвонить коллеге по короткому номеру (имени), не зная его мобильный (IP-адрес).

**Команды:**
```bash
docker network create mynet    # Создать сеть
docker network ls              # Список сетей
docker run --network mynet ... # Подключить контейнер
```

---

### DNS
**Что это:** Domain Name System — система преобразования имён в адреса.

**Простыми словами:** DNS — это "телефонная книга" интернета. Ты вводишь google.com, DNS находит IP-адрес 142.250.74.46.

**В Docker:** Контейнеры в одной сети могут обращаться друг к другу по имени. Docker автоматически создаёт DNS-записи.

```bash
# Внутри контейнера frontend:
ping backend  # Работает благодаря Docker DNS
```

---

### Proxy (Прокси)
**Что это:** Посредник, который пересылает запросы.

**Простыми словами:** Прокси принимает запрос и пересылает его дальше. В нашем проекте nginx работает как прокси — он принимает запросы /api/* и пересылает их в backend.

**Аналогия:** Секретарь — все звонки сначала идут к нему, а он переключает на нужного сотрудника.

---

### CORS
**Что это:** Cross-Origin Resource Sharing — политика безопасности браузера.

**Простыми словами:** Браузер по умолчанию запрещает JavaScript на одном сайте делать запросы на другой сайт. Это защита от атак.

**Проблема:**
- Frontend: http://localhost:3000
- Backend: http://localhost:8080
- Браузер блокирует: "Разные порты = разные origin!"

**Решение:** Использовать прокси. Все запросы идут на frontend (порт 3000), nginx пересылает /api/* в backend. Для браузера всё на одном origin.

---

## Флаги команд Docker

### -d (--detach)
**Что делает:** Запустить в фоновом режиме.

**Без -d:** Терминал занят выводом логов контейнера.
**С -d:** Контейнер работает в фоне, терминал свободен.

```bash
docker run -d nginx
```

---

### -p (--publish)
**Что делает:** Пробросить порт.

**Формат:** `-p хост:контейнер`

```bash
docker run -p 8080:80 nginx
# localhost:8080 → контейнер:80
```

---

### -t (--tag)
**Что делает:** Присвоить имя и тег образу.

```bash
docker build -t myapp:v1 .
# myapp — имя
# v1 — тег (версия)
```

---

### --name
**Что делает:** Присвоить имя контейнеру.

```bash
docker run --name mycontainer nginx
# Вместо случайного ID можно использовать имя
docker stop mycontainer
```

---

### --network
**Что делает:** Подключить контейнер к сети.

```bash
docker run --network mynet nginx
```

---

### --rm
**Что делает:** Удалить контейнер после остановки.

```bash
docker run --rm nginx
# Контейнер автоматически удалится когда остановится
```

---

### -it
**Что делает:** Интерактивный режим с терминалом.

**-i** (--interactive) — держать STDIN открытым
**-t** (--tty) — выделить псевдотерминал

```bash
docker exec -it mycontainer sh
# Войти внутрь контейнера с интерактивным shell
```

---

## Docker Compose термины

### Docker Compose
**Что это:** Инструмент для управления несколькими контейнерами.

**Простыми словами:** Вместо запуска каждого контейнера отдельной командой, описываешь всё в одном файле и запускаешь одной командой.

**Аналогия:** Дирижёр оркестра — управляет всеми музыкантами из одного места.

```bash
docker compose up      # Запустить всё
docker compose down    # Остановить всё
```

---

### docker-compose.yml
**Что это:** Файл конфигурации Docker Compose.

**Простыми словами:** YAML-файл, где описано: какие сервисы запустить, какие порты пробросить, какие сети создать.

```yaml
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
```

---

### Service (Сервис)
**Что это:** Один компонент в Compose.

**Простыми словами:** Каждый сервис — это один контейнер (или несколько копий контейнера). Сервис определяет: из чего собрать, какие порты, какие зависимости.

---

### depends_on
**Что это:** Зависимость между сервисами.

**Простыми словами:** Указывает порядок запуска. `depends_on: backend` означает "запусти backend перед этим сервисом".

```yaml
frontend:
  depends_on:
    - backend
```

---

### healthcheck
**Что это:** Проверка здоровья контейнера.

**Простыми словами:** Docker периодически выполняет команду, чтобы убедиться что приложение работает. Если несколько проверок не прошли — контейнер считается "нездоровым".

```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
  interval: 10s
  timeout: 5s
  retries: 3
```

---

## Dockerfile инструкции

### FROM
**Что делает:** Указывает базовый образ.

```dockerfile
FROM python:3.12
```
Образ наследуется от python:3.12.

---

### WORKDIR
**Что делает:** Устанавливает рабочую директорию.

```dockerfile
WORKDIR /app
```
Все команды выполняются в /app.

---

### COPY
**Что делает:** Копирует файлы с хоста в образ.

```dockerfile
COPY . /app
```
Копировать всё из текущей директории в /app.

---

### RUN
**Что делает:** Выполняет команду во время сборки.

```dockerfile
RUN npm install
```
Устанавливает зависимости Node.js.

---

### CMD
**Что делает:** Команда при запуске контейнера.

```dockerfile
CMD ["python", "app.py"]
```
При старте контейнера выполнится `python app.py`.

---

### EXPOSE
**Что делает:** Документирует порт приложения.

```dockerfile
EXPOSE 8080
```
⚠️ Не открывает порт! Только документация. Реальный проброс через `-p`.

---

### ENV
**Что делает:** Устанавливает переменную окружения.

```dockerfile
ENV NODE_ENV=production
```

---

## Термины разработки

### Backend
**Что это:** Серверная часть приложения.

**Простыми словами:** Код, который работает на сервере: обрабатывает данные, работает с базой данных, выполняет бизнес-логику. Пользователь его не видит напрямую.

---

### Frontend
**Что это:** Клиентская часть приложения.

**Простыми словами:** То, что видит пользователь в браузере: кнопки, формы, картинки, анимации. Работает в браузере пользователя.

---

### API
**Что это:** Application Programming Interface — интерфейс для взаимодействия программ.

**Простыми словами:** API — это "язык общения" между программами. Frontend спрашивает у backend данные через API, backend отвечает.

---

### REST API
**Что это:** Стиль построения API на основе HTTP.

**Простыми словами:** Данные передаются через стандартные HTTP-запросы:
- GET — получить данные
- POST — создать/отправить данные
- PUT — обновить данные
- DELETE — удалить данные

---

### JSON
**Что это:** JavaScript Object Notation — формат данных.

**Простыми словами:** Текстовый формат для передачи структурированных данных. Легко читается и людьми, и программами.

```json
{
  "name": "John",
  "age": 30,
  "city": "New York"
}
```

---

## Термины тестирования

### Smoke Test
**Что это:** Быстрая базовая проверка.

**Простыми словами:** Название от электроники: "включил устройство — не задымилось — работает". Smoke-тест проверяет только самое базовое: "приложение вообще запускается?".

---

### Unit Test
**Что это:** Тест одной маленькой части кода.

**Простыми словами:** Проверяет одну функцию изолированно. Например, функция сложения: 2+2=4.

---

### Integration Test
**Что это:** Тест взаимодействия компонентов.

**Простыми словами:** Проверяет что несколько частей работают вместе. Например, frontend правильно отправляет запрос в backend.

---

### Selenium
**Что это:** Инструмент для автоматизации браузера.

**Простыми словами:** Selenium управляет настоящим браузером программно: открывает страницы, кликает кнопки, заполняет формы — как настоящий пользователь.

---

### pytest
**Что это:** Фреймворк для тестирования на Python.

**Простыми словами:** Библиотека, которая помогает писать и запускать тесты. Предоставляет assert, fixtures, параметризацию и отчёты.

---

### fixture
**Что это:** Подготовка данных для теста.

**Простыми словами:** Функция, которая создаёт что-то нужное для теста (подключение к базе, браузер, тестовые данные) и убирает после теста.

```python
@pytest.fixture
def browser():
    driver = create_browser()
    yield driver  # Тест использует браузер
    driver.quit()  # После теста закрываем
```

---

### assertion
**Что это:** Проверка условия в тесте.

**Простыми словами:** Утверждение "я ожидаю, что это правда". Если неправда — тест падает.

```python
assert result == 42  # "Я утверждаю, что result равен 42"
```

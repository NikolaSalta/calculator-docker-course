# 👨‍🏫 Инструкция для преподавателя

## 📋 Оглавление

1. [Подготовка к уроку](#подготовка-к-уроку)
2. [Демонстрация проекта](#демонстрация-проекта)
3. [Практическая часть](#практическая-часть)
4. [Тестирование](#тестирование)
5. [Отладка и troubleshooting](#отладка-и-troubleshooting)
6. [Дополнительные материалы](#дополнительные-материалы)

---

## 🎯 Подготовка к уроку

### 1. Проверка окружения (5 минут до урока)

**Цель:** Убедиться, что Docker работает на вашем компьютере.

```bash
# Проверить версию Docker
docker --version
# Ожидаемый результат: Docker version 24.0.0 или выше

# Проверить, что Docker Daemon запущен
docker ps
# Ожидаемый результат: пустой список контейнеров (или список запущенных)

# Если ошибка "Cannot connect to Docker daemon":
# - Mac/Windows: Запустите Docker Desktop
# - Linux: sudo systemctl start docker
```

**Зачем:** Если Docker не работает, урок не состоится. Лучше проверить заранее.

---

### 2. Клонирование проекта (если ещё не сделано)

**Цель:** Получить исходный код проекта на свой компьютер.

```bash
# Перейти в рабочую директорию
cd ~/Desktop

# Клонировать репозиторий
git clone https://github.com/NikolaSalta/calculator-docker-course.git

# Перейти в папку проекта
cd calculator-docker-course
```

**Зачем:** Весь код урока находится в этом репозитории. Без него урок невозможен.

---

### 3. Очистка предыдущих запусков (опционально)

**Цель:** Удалить старые контейнеры и образы, чтобы начать с чистого листа.

```bash
# Остановить и удалить все контейнеры проекта
docker compose down

# Удалить образы проекта (опционально, если хотите пересобрать с нуля)
docker rmi calc-backend:local calc-frontend:local

# Удалить сеть проекта (если создавали вручную)
docker network rm calc-net 2>/dev/null || true
```

**Зачем:** Старые контейнеры могут конфликтовать с новыми. Чистое окружение = меньше проблем.

---

## 🚀 Демонстрация проекта

### Сценарий 1: Быстрый старт (для демонстрации работающего приложения)

**Цель:** Показать студентам, как выглядит готовое приложение за 2 минуты.

```bash
# 1. Запустить всё приложение одной командой
docker compose up --build -d

# Что происходит:
# - Docker собирает образы backend и frontend
# - Создаёт сеть для контейнеров
# - Запускает контейнеры в фоновом режиме (-d)
# - Настраивает проброс портов

# 2. Проверить, что контейнеры запустились
docker ps

# Ожидаемый результат: 2 контейнера (calc-backend, calc-frontend)
# CONTAINER ID   IMAGE                    STATUS         PORTS
# abc123...      calc-frontend:latest     Up 10 seconds  0.0.0.0:3000->80/tcp
# def456...      calc-backend:latest      Up 10 seconds  0.0.0.0:8080->8080/tcp

# 3. Проверить health endpoint backend
curl http://localhost:8080/api/health

# Ожидаемый результат: {"status":"OK","service":"calc-backend"}

# 4. Открыть приложение в браузере
open http://localhost:3000
# Windows: start http://localhost:3000
# Linux: xdg-open http://localhost:3000
```

**Что показать студентам:**
- Введите `10 + 5`, нажмите "Вычислить" → результат `15`
- Попробуйте деление на ноль → сообщение об ошибке
- Покажите, что всё работает без установки Java или Node.js

**Зачем:** Студенты должны увидеть конечный результат, чтобы понять, к чему они стремятся.

---

### Сценарий 2: Пошаговая сборка (для обучения)

**Цель:** Показать, как Docker работает "под капотом", собирая и запуская контейнеры вручную.

#### Шаг 1: Создание сети

```bash
# Создать виртуальную сеть для контейнеров
docker network create calc-net

# Зачем: Контейнеры в одной сети могут общаться по именам
# Без сети frontend не сможет найти backend
```

#### Шаг 2: Сборка backend

```bash
# Собрать образ backend из Dockerfile
docker build -t calc-backend:local ./backend

# Что происходит:
# 1. Docker читает backend/Dockerfile
# 2. Скачивает базовый образ (eclipse-temurin:17-jdk)
# 3. Копирует pom.xml и скачивает зависимости Maven
# 4. Копирует исходный код и компилирует Java
# 5. Создаёт финальный образ с JRE и JAR-файлом

# Проверить, что образ создан
docker images | grep calc-backend

# Ожидаемый результат:
# calc-backend   local   abc123...   2 minutes ago   300MB
```

**Объяснить студентам:**
- Образ — это "рецепт" приложения
- Dockerfile — это инструкции для создания образа
- Multi-stage build экономит место (не включаем Maven в финальный образ)

#### Шаг 3: Запуск backend

```bash
# Запустить контейнер backend
docker run -d \
  --name calc-backend \
  --network calc-net \
  -p 8080:8080 \
  calc-backend:local

# Разбор команды:
# -d                    = detached (фоновый режим)
# --name calc-backend   = имя контейнера (для удобства)
# --network calc-net    = подключить к сети calc-net
# -p 8080:8080          = проброс порта (хост:контейнер)
# calc-backend:local    = какой образ использовать

# Проверить логи
docker logs calc-backend

# Ожидаемый результат: "Started CalcApplication in X seconds"

# Проверить работу API
curl http://localhost:8080/api/health
curl -X POST http://localhost:8080/api/calc \
  -H "Content-Type: application/json" \
  -d '{"a":10,"b":5,"op":"+"}'

# Ожидаемый результат: {"result":15.0,"operation":"10.0 + 5.0"}
```

**Объяснить студентам:**
- Контейнер — это запущенный экземпляр образа
- Порты нужно пробрасывать, чтобы обращаться к приложению снаружи
- Без сети контейнеры изолированы друг от друга

#### Шаг 4: Сборка frontend

```bash
# Собрать образ frontend
docker build -t calc-frontend:local ./frontend

# Что происходит:
# 1. Stage 1 (builder): Node.js собирает React-приложение
# 2. Stage 2 (production): Nginx копирует собранные файлы
# 3. Финальный образ содержит только Nginx + статические файлы

# Проверить образ
docker images | grep calc-frontend
```

#### Шаг 5: Запуск frontend

```bash
# Запустить контейнер frontend
docker run -d \
  --name calc-frontend \
  --network calc-net \
  -p 3000:80 \
  calc-frontend:local

# Разбор:
# -p 3000:80  = порт 3000 на хосте → порт 80 в контейнере (Nginx)

# Проверить логи
docker logs calc-frontend

# Открыть в браузере
open http://localhost:3000
```

**Объяснить студентам:**
- Frontend обращается к backend по имени `calc-backend` (DNS в Docker сети)
- Nginx проксирует запросы `/api/*` в backend (решает проблему CORS)
- Без прокси браузер заблокирует запросы (разные порты = разные origins)

---

## 🧪 Практическая часть

### Задание 1: Изменить код и пересобрать

**Цель:** Показать, что изменения кода требуют пересборки образа.

```bash
# 1. Студенты изменяют код (например, в CalcController.java)
# Пример: изменить сообщение в health endpoint

# 2. Остановить старый контейнер
docker stop calc-backend
docker rm calc-backend

# 3. Пересобрать образ
docker build -t calc-backend:local ./backend

# 4. Запустить новый контейнер
docker run -d --name calc-backend --network calc-net -p 8080:8080 calc-backend:local

# 5. Проверить изменения
curl http://localhost:8080/api/health
```

**Зачем:** Студенты должны понять цикл разработки с Docker.

---

### Задание 2: Отладка через логи

**Цель:** Научить находить ошибки через логи контейнеров.

```bash
# Посмотреть логи в реальном времени
docker logs -f calc-backend

# Зачем: Если приложение падает, логи покажут причину

# Найти ошибку (например, NullPointerException)
# Исправить код → пересобрать → перезапустить
```

---

### Задание 3: Зайти внутрь контейнера

**Цель:** Показать, что контейнер — это изолированная Linux-система.

```bash
# Зайти в контейнер backend
docker exec -it calc-backend sh

# Внутри контейнера:
ls -la                    # Посмотреть файлы
ps aux                    # Посмотреть процессы
cat /app/application.jar  # Посмотреть JAR-файл (бинарный)
exit                      # Выйти

# Зайти в контейнер frontend
docker exec -it calc-frontend sh

# Внутри:
ls /usr/share/nginx/html  # Статические файлы React
cat /etc/nginx/nginx.conf # Конфигурация Nginx
exit
```

**Зачем:** Студенты должны понять, что контейнер — не "чёрный ящик", а обычная Linux-система.

---

## 🧪 Тестирование

### Тест 1: API-тесты (без UI)

**Цель:** Проверить, что backend API работает корректно.

```bash
# Запустить API-тесты
docker compose -f docker-compose.test.yml up --build --abort-on-container-exit

# Что происходит:
# 1. Запускаются backend + frontend
# 2. Запускается контейнер с pytest
# 3. Тесты проверяют API endpoints
# 4. Контейнеры останавливаются после завершения

# Ожидаемый результат:
# ======================== 10 passed in 5.23s ========================

# Посмотреть, какие тесты прошли
docker compose -f docker-compose.test.yml logs tests | grep PASSED
```

**Объяснить студентам:**
- Smoke tests: базовая проверка (приложение запускается?)
- API tests: проверка бизнес-логики (сложение, деление, ошибки)
- Integration tests: проверка взаимодействия frontend ↔ backend

---

### Тест 2: UI-тесты с Selenium

**Цель:** Проверить, что UI работает в реальном браузере.

```bash
# Запустить полные тесты (API + UI)
docker compose -f docker-compose.selenium.yml up --build --abort-on-container-exit

# Что происходит:
# 1. Запускаются backend + frontend
# 2. Запускается Selenium Grid (координатор браузеров)
# 3. Запускается Chrome в контейнере
# 4. Pytest управляет Chrome через Selenium
# 5. Тесты кликают кнопки, вводят числа, проверяют результаты

# Ожидаемый результат:
# ======================== 18 passed in 116.46s ========================

# Посмотреть HTML-отчёт
open ./test-reports/report.html
```

**Показать студентам отчёт:**
- Какие тесты прошли
- Сколько времени заняло выполнение
- Детали каждого теста (что отправлялось, что получено)

**Зачем:** Автоматизация тестирования экономит время и ловит баги до продакшена.

---

### Тест 3: Frontend unit-тесты

**Цель:** Проверить React-компоненты в изоляции.

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости (если ещё не установлены)
npm install

# Запустить unit-тесты
npm test -- --watchAll=false

# Ожидаемый результат:
# PASS  src/App.test.js
#   ✓ renders calculator title (50ms)
#   ✓ performs addition correctly (120ms)
#   ✓ handles division by zero (80ms)

# Вернуться в корень проекта
cd ..
```

**Зачем:** Unit-тесты проверяют отдельные компоненты без запуска всего приложения.

---

## 🐛 Отладка и troubleshooting

### Проблема 1: Порт уже занят

**Симптом:**
```
Error: bind: address already in use
```

**Решение:**
```bash
# Найти процесс, занимающий порт 8080
lsof -i :8080
# или
netstat -an | grep 8080

# Убить процесс
kill -9 <PID>

# Или изменить порт в docker-compose.yml:
# ports:
#   - "8081:8080"  # вместо 8080:8080
```

---

### Проблема 2: Контейнер не запускается

**Симптом:**
```
docker ps  # контейнер не в списке
```

**Решение:**
```bash
# Посмотреть все контейнеры (включая остановленные)
docker ps -a

# Посмотреть логи
docker logs calc-backend

# Типичные причины:
# - Ошибка в коде (NullPointerException)
# - Неправильный Dockerfile
# - Порт уже занят
```

---

### Проблема 3: Frontend не видит backend

**Симптом:**
```
Network Error или CORS error в браузере
```

**Решение:**
```bash
# 1. Проверить, что backend запущен
docker ps | grep backend

# 2. Проверить, что backend отвечает
curl http://localhost:8080/api/health

# 3. Проверить, что контейнеры в одной сети
docker inspect calc-backend | grep NetworkMode
docker inspect calc-frontend | grep NetworkMode

# 4. Проверить конфигурацию Nginx
docker exec calc-frontend cat /etc/nginx/nginx.conf | grep proxy_pass
# Должно быть: proxy_pass http://backend:8080;
```

---

### Проблема 4: Тесты падают

**Симптом:**
```
FAILED test_api.py::test_addition
```

**Решение:**
```bash
# 1. Посмотреть детали ошибки
docker compose -f docker-compose.test.yml logs tests

# 2. Запустить тесты с подробным выводом
docker compose -f docker-compose.test.yml run --rm tests pytest -v -s

# 3. Проверить, что сервисы готовы
docker compose -f docker-compose.test.yml logs backend | grep "Started"
```

---

## 📚 Дополнительные материалы

### Для студентов

1. **Интерактивный квиз:**
   - Онлайн: https://nikolasalta.github.io/calculator-docker-course/
   - Локально: открыть `documents/docker_quiz.html`
   - 64 вопроса, 3 режима (флешкарты, тест, ввод текста)

2. **Глоссарий:**
   - Файл: `GLOSSARY.md`
   - Все термины Docker с простыми объяснениями

3. **План урока:**
   - Файл: `LESSON.md`
   - Подробный план на 75 минут

4. **Диаграммы:**
   - Папка: `documents/pdf_diagrams/`
   - Визуальные схемы архитектуры Docker

---

### Полезные команды для демонстрации

```bash
# Показать все образы
docker images

# Показать все контейнеры
docker ps -a

# Показать использование ресурсов
docker stats

# Показать сети
docker network ls

# Показать детали контейнера
docker inspect calc-backend

# Показать историю образа (слои)
docker history calc-backend:local

# Удалить всё (ОСТОРОЖНО!)
docker system prune -a
```

---

## 🎓 Рекомендации по проведению урока

### Тайминг (75 минут)

1. **Введение (10 мин):**
   - Что такое Docker и зачем он нужен
   - Показать готовое приложение (docker compose up)

2. **Теория (15 мин):**
   - Образы vs контейнеры
   - Dockerfile
   - Сети и порты
   - Использовать диаграммы из `documents/pdf_diagrams/`

3. **Практика: Manual режим (25 мин):**
   - Создать сеть
   - Собрать backend
   - Запустить backend
   - Собрать frontend
   - Запустить frontend
   - Студенты повторяют за вами

4. **Практика: Docker Compose (10 мин):**
   - Показать docker-compose.yml
   - Запустить одной командой
   - Объяснить преимущества

5. **Тестирование (10 мин):**
   - Запустить API-тесты
   - Запустить UI-тесты
   - Показать отчёт

6. **Q&A и закрепление (5 мин):**
   - Ответы на вопросы
   - Дать ссылку на квиз для самопроверки

---

## 📝 Чек-лист перед уроком

- [ ] Docker Desktop запущен
- [ ] Проект склонирован
- [ ] `docker ps` работает без ошибок
- [ ] Порты 3000, 8080, 4444 свободны
- [ ] Браузер открыт
- [ ] Терминал готов к демонстрации
- [ ] Диаграммы открыты для показа
- [ ] Ссылка на квиз готова для отправки студентам

---

## 🆘 Контакты и поддержка

- **Репозиторий:** https://github.com/NikolaSalta/calculator-docker-course
- **Онлайн-квиз:** https://nikolasalta.github.io/calculator-docker-course/

Удачного урока! 🚀


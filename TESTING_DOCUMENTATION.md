# 🧪 Полная документация по тестированию

> **Автор:** Docker Calculator Project  
> **Технологии:** Python, pytest, Selenium, Docker  
> **Дата:** December 2025

---

## 📋 Содержание

1. [Обзор тестирования](#1-обзор-тестирования)
2. [Технологии и инструменты](#2-технологии-и-инструменты)
3. [Архитектура тестов](#3-архитектура-тестов)
4. [API-тесты (test_api.py)](#4-api-тесты)
5. [UI-тесты Selenium (test_ui_selenium.py)](#5-ui-тесты-selenium)
6. [Конфигурация (conftest.py)](#6-конфигурация)
7. [Запуск тестов](#7-запуск-тестов)
8. [HTML-отчёты](#8-html-отчёты)
9. [Docker-образ тестов](#9-docker-образ-тестов)
10. [🎬 Визуальное тестирование (noVNC)](#10--визуальное-тестирование-novnc)

---

## 1. Обзор тестирования

### Что тестируется?

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CALCULATOR APPLICATION                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────────┐          ┌──────────────────┐                         │
│  │     FRONTEND     │          │     BACKEND      │                         │
│  │  (React + Nginx) │  ─────►  │  (Spring Boot)   │                         │
│  │                  │   /api   │                  │                         │
│  │  ┌────────────┐  │          │  ┌────────────┐  │                         │
│  │  │   React    │  │          │  │ Controller │  │                         │
│  │  │   App.js   │  │          │  │  /api/calc │  │                         │
│  │  └────────────┘  │          │  │  /api/health│ │                         │
│  │  ┌────────────┐  │          │  └────────────┘  │                         │
│  │  │   Nginx    │  │          │                  │                         │
│  │  │   Proxy    │  │          │                  │                         │
│  │  └────────────┘  │          │                  │                         │
│  └──────────────────┘          └──────────────────┘                         │
│          ▲                              ▲                                    │
│          │                              │                                    │
│  ┌───────┴──────────────────────────────┴───────┐                           │
│  │                  ТЕСТЫ                        │                           │
│  │  ┌────────────────┐    ┌──────────────────┐  │                           │
│  │  │  API-тесты     │    │  UI-тесты        │  │                           │
│  │  │  (requests)    │    │  (Selenium)      │  │                           │
│  │  │                │    │                  │  │                           │
│  │  │  test_api.py   │    │  test_ui_*.py    │  │                           │
│  │  └────────────────┘    └──────────────────┘  │                           │
│  └───────────────────────────────────────────────┘                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Типы тестов

| Тип | Файл | Количество | Описание |
|-----|------|------------|----------|
| **Smoke** | `test_api.py` | 2 | Базовая проверка работоспособности |
| **API** | `test_api.py` | 8 | HTTP-запросы к backend |
| **UI (Selenium)** | `test_ui_selenium.py` | 8 | Тесты через браузер |
| **Всего** | — | **18** | — |

### Пирамида тестирования

```
                    ▲
                   /┃\          UI-тесты (Selenium)
                  / ┃ \         ├── Медленные (10+ сек)
                 /  ┃  \        ├── Хрупкие (зависят от UI)
                /   ┃   \       └── 8 тестов
               /    ┃    \
              /─────┃─────\
             /      ┃      \    API-тесты
            /       ┃       \   ├── Быстрые (< 1 сек)
           /        ┃        \  ├── Стабильные
          /         ┃         \ └── 8 тестов
         /──────────┃──────────\
        /           ┃           \   Smoke-тесты
       /            ┃            \  ├── Очень быстрые
      /             ┃             \ ├── Проверяют "живой ли сервис"
     /              ┃              \└── 2 теста
    ──────────────────────────────────
```

---

## 2. Технологии и инструменты

### Почему pytest?

**pytest** — самый популярный фреймворк тестирования для Python.

| Преимущество | Описание |
|--------------|----------|
| **Простой синтаксис** | Просто `assert` вместо `self.assertEqual()` |
| **Fixtures** | Мощная система подготовки тестов |
| **Параметризация** | Один тест с множеством наборов данных |
| **Плагины** | 800+ плагинов (html-отчёты, coverage, timeout) |
| **Автообнаружение** | Находит тесты по паттерну `test_*` |

```python
# unittest (старый стиль)
class TestCalculator(unittest.TestCase):
    def test_addition(self):
        self.assertEqual(10 + 5, 15)

# pytest (современный стиль)
def test_addition():
    assert 10 + 5 == 15  # Просто и понятно!
```

### Почему Selenium?

**Selenium** — инструмент для автоматизации браузера.

```
┌─────────────────────────────────────────────────────────────────┐
│                      SELENIUM ARCHITECTURE                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌──────────────┐         ┌───────────────┐                    │
│   │   ТЕСТЫ      │         │  SELENIUM HUB │                    │
│   │  (Python)    │  ─────► │  (контейнер)  │                    │
│   │              │  HTTP   │   :4444       │                    │
│   └──────────────┘         └───────┬───────┘                    │
│                                    │                            │
│                          ┌─────────┴─────────┐                  │
│                          ▼                   ▼                  │
│                 ┌────────────────┐  ┌────────────────┐          │
│                 │ CHROME NODE   │  │ FIREFOX NODE   │          │
│                 │ (контейнер)   │  │ (контейнер)    │          │
│                 │               │  │                │          │
│                 │ ┌──────────┐  │  │ ┌──────────┐   │          │
│                 │ │  Chrome  │  │  │ │ Firefox  │   │          │
│                 │ │ Browser  │  │  │ │ Browser  │   │          │
│                 │ └──────────┘  │  │ └──────────┘   │          │
│                 └────────────────┘  └────────────────┘          │
│                          │                                      │
│                          ▼                                      │
│              ┌─────────────────────────┐                        │
│              │    ТЕСТИРУЕМОЕ          │                        │
│              │    ПРИЛОЖЕНИЕ           │                        │
│              │    (Frontend + Backend) │                        │
│              └─────────────────────────┘                        │
└─────────────────────────────────────────────────────────────────┘
```

**Почему Selenium Grid (а не локальный браузер)?**

| Аспект | Локальный браузер | Selenium Grid |
|--------|-------------------|---------------|
| **Настройка** | Нужен ChromeDriver | Всё в Docker |
| **Изоляция** | Конфликт версий | Версия фиксирована |
| **CI/CD** | Сложно настроить | Работает везде |
| **Параллелизм** | 1 браузер | Много нод |

### Используемые библиотеки

```
┌─────────────────────────────────────────────────────────────────┐
│                     requirements.txt                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  pytest>=8.0.0           # Фреймворк тестирования               │
│  pytest-html>=4.0.0      # HTML-отчёты                          │
│  pytest-timeout>=2.0.0   # Таймауты для тестов                  │
│                                                                  │
│  requests>=2.31.0        # HTTP-клиент для API-тестов           │
│                                                                  │
│  selenium>=4.15.0        # Автоматизация браузера               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. Архитектура тестов

### Структура файлов

```
tests/
├── conftest.py          # Общие fixtures (browser, wait_for_services)
├── test_config.py       # Конфигурация (URL, таймауты)
├── test_api.py          # API-тесты
├── test_ui_selenium.py  # UI-тесты с Selenium
├── pytest.ini           # Настройки pytest
├── requirements.txt     # Python-зависимости
└── Dockerfile           # Docker-образ для тестов
```

### Конфигурационные файлы

```yaml
# docker-compose.test.yml — Только API-тесты
services:
  backend:   # Java Spring Boot
  frontend:  # React + Nginx
  tests:     # pytest + requests

# docker-compose.selenium.yml — API + UI-тесты
services:
  backend:        # Java Spring Boot
  frontend:       # React + Nginx
  selenium-hub:   # Selenium Hub
  selenium-chrome: # Chrome в контейнере
  tests:          # pytest + selenium
```

### Жизненный цикл теста

```
┌─────────────────────────────────────────────────────────────────┐
│                    ЖИЗНЕННЫЙ ЦИКЛ ТЕСТА                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. СБОР (Collection)                                            │
│     pytest сканирует test_*.py и находит test_* функции         │
│                                                                  │
│  2. ПОДГОТОВКА (Setup)                                           │
│     ├── wait_for_services()  # session scope, autouse            │
│     │   └── Ждём backend и frontend                              │
│     └── browser()            # function scope                    │
│         └── Создаём WebDriver для теста                          │
│                                                                  │
│  3. ВЫПОЛНЕНИЕ (Call)                                            │
│     └── Тест выполняется                                         │
│                                                                  │
│  4. ОЧИСТКА (Teardown)                                           │
│     └── browser.quit()  # Закрываем браузер                      │
│                                                                  │
│  5. ОТЧЁТ (Report)                                               │
│     └── pytest-html генерирует report.html                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. API-тесты

### Файл: `test_api.py`

#### 4.1 Smoke-тесты

**Что такое Smoke-тест?**

> Название пришло из электроники: "Включи устройство — если не задымилось, базовый тест пройден".

```python
class TestSmoke:
    """Быстрая проверка: система вообще работает?"""
    
    @pytest.mark.smoke
    def test_backend_health(self):
        """Проверяет /api/health endpoint"""
        resp = requests.get(f"{BACKEND_URL}/api/health")
        assert resp.status_code == 200
        assert resp.json()["status"] == "OK"
```

| Тест | Что проверяет | Время |
|------|---------------|-------|
| `test_backend_health` | Backend отвечает на health-check | ~12ms |
| `test_frontend_available` | Frontend отдаёт HTML | ~2ms |

#### 4.2 Параметризованные тесты

**Как создавались:**

Вместо 6 отдельных функций используется `@pytest.mark.parametrize`:

```python
@pytest.mark.parametrize("a,b,op,expected", [
    (10, 5, "+", 15.0),   # Сложение
    (10, 5, "-", 5.0),    # Вычитание
    (10, 5, "*", 50.0),   # Умножение
    (10, 5, "/", 2.0),    # Деление
    (-5, 3, "+", -2.0),   # Отрицательные
    (0, 100, "*", 0.0),   # Умножение на 0
])
def test_operations(self, a, b, op, expected):
    """Один тест — 6 наборов данных"""
    payload = {"a": a, "b": b, "op": op}
    resp = requests.post(f"{BACKEND_URL}/api/calc", json=payload)
    assert resp.json()["result"] == pytest.approx(expected)
```

**Результат в отчёте:**
```
test_operations[10-5-+-15.0] PASSED
test_operations[10-5---5.0] PASSED
test_operations[10-5-*-50.0] PASSED
test_operations[10-5-/-2.0] PASSED
test_operations[-5-3-+--2.0] PASSED
test_operations[0-100-*-0.0] PASSED
```

#### 4.3 Тест деления на ноль

**Почему это важный тест?**

```python
def test_division_by_zero(self):
    """
    Edge case: деление на ноль.
    
    Ожидаем:
    - HTTP 400 Bad Request (валидационная ошибка)
    - Понятное сообщение об ошибке
    - НЕ 500 Internal Server Error (не крашится)
    """
    resp = requests.post(
        f"{BACKEND_URL}/api/calc",
        json={"a": 10, "b": 0, "op": "/"}
    )
    assert resp.status_code == 400  # Не 500!
    assert "zero" in resp.json()["message"].lower()
```

#### 4.4 Интеграционный тест прокси

```python
def test_frontend_proxy(self):
    """
    Проверяет: nginx корректно проксирует /api в backend.
    
    Путь запроса:
    Тест → Frontend (nginx) → Backend → обратно
    """
    # ВАЖНО: запрос на FRONTEND, не backend!
    resp = requests.post(
        f"{FRONTEND_URL}/api/calc",  # http://frontend:80/api/calc
        json={"a": 7, "b": 3, "op": "*"}
    )
    assert resp.json()["result"] == 21.0
```

---

## 5. UI-тесты Selenium

### Файл: `test_ui_selenium.py`

#### 5.1 Page Object Pattern

**Что это и зачем?**

Page Object — паттерн, который инкапсулирует UI-элементы и действия в отдельный класс.

```
┌─────────────────────────────────────────────────────────────────┐
│                   БЕЗ PAGE OBJECT (плохо)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  def test_addition():                                            │
│      driver.find_element(By.CSS_SELECTOR, "input").send_keys(10)│
│      driver.find_element(By.CSS_SELECTOR, "select").click()     │
│      driver.find_element(By.CSS_SELECTOR, "button").click()     │
│      # Локаторы везде дублируются!                               │
│                                                                  │
│  def test_subtraction():                                         │
│      driver.find_element(By.CSS_SELECTOR, "input").send_keys(5) │
│      # Те же локаторы снова...                                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   С PAGE OBJECT (хорошо)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  class CalculatorPage:                                           │
│      def enter_numbers(self, a, b): ...                          │
│      def select_operation(self, op): ...                         │
│      def click_calculate(self): ...                              │
│      def get_result(self): ...                                   │
│                                                                  │
│  def test_addition(browser):                                     │
│      page = CalculatorPage(browser).open()                       │
│      page.enter_numbers(10, 5).select_operation("+")             │
│      page.click_calculate()                                      │
│      assert page.get_result() == 15.0                            │
│                                                                  │
│  # Локаторы в ОДНОМ месте!                                       │
│  # Если UI изменится — меняем только CalculatorPage              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 5.2 Реализация CalculatorPage

```python
class CalculatorPage:
    def __init__(self, driver):
        self.driver = driver
        self.wait = WebDriverWait(driver, 10)
    
    def open(self):
        """Открыть страницу калькулятора"""
        self.driver.get(FRONTEND_URL)
        self.wait.until(
            EC.presence_of_element_located((By.CSS_SELECTOR, "button"))
        )
        return self  # Method chaining
    
    def enter_numbers(self, a, b):
        """Ввести два числа"""
        inputs = self.driver.find_elements(
            By.CSS_SELECTOR, "input[type='number']"
        )
        inputs[0].clear()
        inputs[0].send_keys(str(a))
        inputs[1].clear()
        inputs[1].send_keys(str(b))
        return self
    
    def select_operation(self, op):
        """Выбрать операцию (+, -, *, /)"""
        select = Select(
            self.driver.find_element(By.CSS_SELECTOR, "select")
        )
        select.select_by_value(op)
        return self
    
    def click_calculate(self):
        """Нажать кнопку и дождаться результата"""
        self.driver.find_element(By.CSS_SELECTOR, "button").click()
        self.wait.until(
            EC.element_to_be_clickable((By.CSS_SELECTOR, "button"))
        )
        return self
    
    def get_result(self):
        """Извлечь результат со страницы"""
        elements = self.driver.find_elements(
            By.XPATH, "//*[contains(text(), 'Результат')]"
        )
        if elements:
            text = elements[0].text  # "Результат: 42"
            match = re.search(r'[-+]?\d*\.?\d+', text.split(':')[-1])
            if match:
                return float(match.group())
        return None
```

#### 5.3 UI-тесты

| Тест | Описание | Проверяет |
|------|----------|-----------|
| `test_page_loads` | Страница загружается | HTML содержит "Калькулятор" |
| `test_addition` | 25 + 17 = 42 | Сложение работает |
| `test_multiplication` | 7 × 8 = 56 | Умножение работает |
| `test_subtraction` | 100 - 37 = 63 | Вычитание работает |
| `test_division` | 144 ÷ 12 = 12 | Деление работает |
| `test_decimal_numbers` | 3.5 + 2.5 = 6.0 | Десятичные числа |
| `test_negative_numbers` | -10 + 5 = -5 | Отрицательные числа |
| `test_sequential_calculations` | Три вычисления подряд | Состояние не ломается |

#### 5.4 Пример теста

```python
@pytest.mark.ui_selenium
class TestCalculatorUI:
    
    def test_multiplication(self, browser):
        """
        Умножение через UI: 7 × 8 = 56.
        
        Это полный end-to-end тест:
        1. Открываем страницу
        2. Вводим числа
        3. Выбираем операцию
        4. Кликаем кнопку
        5. Проверяем результат
        """
        page = CalculatorPage(browser).open()
        page.enter_numbers(7, 8).select_operation("*").click_calculate()
        assert page.get_result() == 56.0
```

---

## 6. Конфигурация

### Файл: `conftest.py`

#### 6.1 Fixture: wait_for_services

```python
@pytest.fixture(scope="session", autouse=True)
def wait_for_services():
    """
    Ждёт готовности сервисов ПЕРЕД ВСЕМИ тестами.
    
    scope="session" — один раз на весь запуск
    autouse=True — выполняется автоматически
    """
    print(f"\n⏳ Ожидание сервисов (до {STARTUP_WAIT} сек)...")
    
    services = [
        (f"{BACKEND_URL}/api/health", "Backend"),
        (FRONTEND_URL, "Frontend"),
    ]
    
    for url, name in services:
        if not wait_for_url(url, STARTUP_WAIT, name):
            pytest.exit(f"❌ {name} не готов: {url}")
    
    print("🚀 Все сервисы готовы!\n")
```

#### 6.2 Fixture: browser

```python
@pytest.fixture(scope="function")
def browser():
    """
    Создаёт браузер для КАЖДОГО теста.
    
    yield — разделяет setup и teardown:
    - До yield: создание браузера
    - После yield: закрытие браузера
    """
    options = ChromeOptions()
    options.add_argument("--headless=new")     # Без GUI
    options.add_argument("--no-sandbox")        # Для Docker
    options.add_argument("--disable-dev-shm-usage")
    options.add_argument("--window-size=1920,1080")
    
    driver = webdriver.Remote(
        command_executor=SELENIUM_HUB_URL,
        options=options
    )
    driver.implicitly_wait(10)
    
    yield driver  # ← Браузер передаётся в тест
    
    driver.quit()  # ← Cleanup после теста
```

### Файл: `test_config.py`

```python
import os

# URL сервисов (переменные окружения или значения по умолчанию)
BACKEND_URL = os.getenv("BACKEND_URL", "http://backend:8080")
FRONTEND_URL = os.getenv("FRONTEND_URL", "http://frontend:80")
SELENIUM_HUB_URL = os.getenv("SELENIUM_HUB_URL", "http://selenium-hub:4444")

# Таймауты
STARTUP_WAIT = int(os.getenv("STARTUP_WAIT", "60"))  # Ожидание сервисов
REQUEST_TIMEOUT = int(os.getenv("REQUEST_TIMEOUT", "10"))  # HTTP-запросы
```

---

## 7. Запуск тестов

### 7.1 Только API-тесты (быстро)

```bash
docker compose -f docker-compose.test.yml up --build --abort-on-container-exit
```

**Время:** ~30 секунд

### 7.2 API + UI-тесты (полный набор)

```bash
docker compose -f docker-compose.selenium.yml up --build --abort-on-container-exit
```

**Время:** ~50 секунд

### 7.3 Выборочный запуск

```bash
# Только smoke-тесты
docker compose -f docker-compose.test.yml run tests pytest -m smoke

# Только API-тесты
docker compose -f docker-compose.test.yml run tests pytest -m api

# Только UI-тесты
docker compose -f docker-compose.selenium.yml run tests pytest -m ui_selenium

# Конкретный файл
docker compose -f docker-compose.test.yml run tests pytest test_api.py -v
```

### 7.4 Маркеры тестов

| Маркер | Описание | Файл |
|--------|----------|------|
| `@pytest.mark.smoke` | Базовая проверка | test_api.py |
| `@pytest.mark.api` | API-тесты | test_api.py |
| `@pytest.mark.ui_selenium` | UI-тесты | test_ui_selenium.py |

---

## 8. HTML-отчёты

### 8.1 Где находятся отчёты?

```
test-reports/
└── report.html    ← Открыть в браузере
```

### 8.2 Автоматический просмотр отчётов

**Вариант 1: Локальный HTTP-сервер**

```bash
cd test-reports
python3 -m http.server 8888
# Открыть http://localhost:8888/report.html
```

**Вариант 2: Docker-сервер отчётов**

```bash
docker compose -f docker-compose.reports.yml up -d
# Открыть http://localhost:9000/report.html
```

### 8.3 Содержимое отчёта

```
┌─────────────────────────────────────────────────────────────────┐
│                     PYTEST HTML REPORT                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Environment:                                                    │
│  ├── Python: 3.12.0                                              │
│  ├── pytest: 8.0.0                                               │
│  └── Plugins: html, timeout                                      │
│                                                                  │
│  Summary:                                                        │
│  ├── 18 passed                                                   │
│  ├── 0 failed                                                    │
│  └── Duration: 50.23s                                            │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Test                          │ Result │ Duration │ Desc   │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ test_backend_health           │ PASSED │ 12ms     │ ...    │ │
│  │ test_frontend_available       │ PASSED │ 2ms      │ ...    │ │
│  │ test_operations[10-5-+-15.0]  │ PASSED │ 57ms     │ ...    │ │
│  │ ...                           │ ...    │ ...      │ ...    │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. Docker-образ тестов

### 9.1 Что внутри образа?

**Версия 2.0** содержит всё необходимое для тестирования:

| Компонент | Версия | Описание |
|-----------|--------|----------|
| **Python** | 3.12 | Язык тестов |
| **Chromium** | latest | Браузер (встроен!) |
| **ChromeDriver** | latest | Драйвер для Selenium |
| **pytest** | 8.0.0 | Фреймворк тестирования |
| **selenium** | 4.17.2 | Управление браузером |
| **pytest-html** | 4.1.1 | HTML-отчёты |

### 9.2 Multi-Platform образы

**📦 Поддерживаемые архитектуры:**

| Архитектура | Платформа | Где используется |
|-------------|-----------|------------------|
| **linux/amd64** | x86_64 | Windows, Linux, Intel Mac |
| **linux/arm64** | aarch64 | Apple Silicon (M1/M2/M3), AWS Graviton |

### 9.3 Docker Hub

**🔗 Ссылки на образы:**

| Тег | Описание | Ссылка |
|-----|----------|--------|
| `latest` | Последняя версия (multi-arch) | [nikolaysaltan/calculator-docker-tests:latest](https://hub.docker.com/r/nikolaysaltan/calculator-docker-tests) |
| `v2.0-chromium` | Версия 2.0 с Chromium | [nikolaysaltan/calculator-docker-tests:v2.0-chromium](https://hub.docker.com/r/nikolaysaltan/calculator-docker-tests/tags?name=v2.0) |
| `v2.0-multiarch` | Multi-platform (AMD64 + ARM64) | [nikolaysaltan/calculator-docker-tests:v2.0-multiarch](https://hub.docker.com/r/nikolaysaltan/calculator-docker-tests/tags?name=multiarch) |

```bash
# Скачать образ (автоматически выберет нужную архитектуру)
docker pull nikolaysaltan/calculator-docker-tests:latest

# Явно указать платформу
docker pull --platform linux/amd64 nikolaysaltan/calculator-docker-tests:latest  # Windows/Linux
docker pull --platform linux/arm64 nikolaysaltan/calculator-docker-tests:latest  # Apple M1/M2/M3
```

### 9.4 Два режима работы

**Режим 1: STANDALONE (встроенный браузер)**

```bash
# Браузер уже внутри контейнера — дополнительных контейнеров не нужно!
docker run --rm \
  -e USE_EMBEDDED_BROWSER=true \
  -e HEADLESS=true \
  -e BACKEND_URL=http://host.docker.internal:8080 \
  -e FRONTEND_URL=http://host.docker.internal:3001 \
  -v $(pwd)/reports:/tests/reports \
  nikolaysaltan/calculator-docker-tests:latest
```

**Режим 2: REMOTE (с noVNC для визуализации)**

```bash
# Браузер в отдельном контейнере — можно видеть тесты!
docker compose -f docker-compose.selenium.yml up --build

# Откройте http://localhost:7900 (пароль: secret)
```

### 9.5 Сборка Multi-Platform образов

```bash
# Создать buildx builder для multi-platform сборки
docker buildx create --name multiplatform --use

# Собрать и загрузить образы для обеих архитектур
docker buildx build --platform linux/amd64,linux/arm64 \
  -t nikolaysaltan/calculator-docker-tests:latest \
  -t nikolaysaltan/calculator-docker-tests:v2.0-multiarch \
  --push ./tests

# Или собрать только для своей архитектуры
docker build -t nikolaysaltan/calculator-docker-tests:v2.0 ./tests
docker push nikolaysaltan/calculator-docker-tests:v2.0
```

### 9.5.1 ДВА ОТДЕЛЬНЫХ Dockerfile (ARM64 и AMD64)

Если вам нужно **2 отдельных Dockerfile**, используйте:
- `tests/Dockerfile.arm64` — под Apple Silicon / ARM64 (база как “в начале”: `python:3.12-slim`)
- `tests/Dockerfile.amd64` — под x86_64 / Windows (Debian Bookworm: `python:3.12-slim-bookworm`)

```bash
# ARM64 (Mac M1/M2/M3)
docker buildx build --platform linux/arm64 -f tests/Dockerfile.arm64 \
  -t nikolaysaltan/calculator-docker-tests:arm64 --push ./tests

# AMD64 / x86_64 (Windows/Linux)
docker buildx build --platform linux/amd64 -f tests/Dockerfile.amd64 \
  -t nikolaysaltan/calculator-docker-tests:amd64 --push ./tests
```

### 9.6 Dockerfile

```dockerfile
FROM python:3.12-slim-bookworm

# Установка Chromium и ChromeDriver
RUN apt-get update && apt-get install -y --no-install-recommends \
    chromium \
    chromium-driver \
    fonts-liberation \
    fonts-noto-color-emoji \
    libnss3 libatk1.0-0 libatk-bridge2.0-0 \
    libcups2 libdrm2 libxkbcommon0 \
    libxcomposite1 libxdamage1 libxfixes3 \
    libxrandr2 libgbm1 libasound2 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /tests

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY pytest.ini conftest.py test_config.py .
COPY test_api.py test_ui_selenium.py .

RUN mkdir -p /tests/reports

ENV PYTHONUNBUFFERED=1
ENV CHROME_BIN=/usr/bin/chromium
ENV CHROMEDRIVER_PATH=/usr/bin/chromedriver
ENV USE_EMBEDDED_BROWSER=true
ENV HEADLESS=true

ENTRYPOINT ["pytest"]
CMD ["-v", "--html=/tests/reports/report.html", "--self-contained-html"]
```

---

## 10. 🎬 Визуальное тестирование (noVNC)

### Что такое noVNC?

**noVNC** — это VNC-клиент в браузере. Позволяет видеть удалённый рабочий стол прямо в браузере!

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ВИЗУАЛЬНОЕ ТЕСТИРОВАНИЕ                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────┐       ┌─────────────────────────────────────────────────┐  │
│  │  Ваш       │       │  Docker контейнер (selenium-chrome)              │  │
│  │  браузер   │       │  ┌───────────────────────────────────────────┐  │  │
│  │            │       │  │  Linux Desktop + Chromium                 │  │  │
│  │  localhost │◄──────│  │  ┌─────────────────────────────────────┐  │  │  │
│  │  :7900     │ noVNC │  │  │     http://frontend:80              │  │  │  │
│  │            │       │  │  │  ┌──────────────────────────────┐   │  │  │  │
│  │  Вы видите │       │  │  │  │      🧮 Калькулятор          │   │  │  │  │
│  │  тесты!    │       │  │  │  │                              │   │  │  │  │
│  └─────────────┘       │  │  │  │  [10] [+] [5] [Вычислить]   │   │  │  │  │
│                        │  │  │  │                              │   │  │  │  │
│                        │  │  │  │  Результат: 15              │   │  │  │  │
│                        │  │  │  └──────────────────────────────┘   │  │  │  │
│                        │  │  └─────────────────────────────────────┘  │  │  │
│                        │  └───────────────────────────────────────────┘  │  │
│                        └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Как использовать

```bash
# 1. Запустить тесты
docker compose -f docker-compose.selenium.yml up --build

# 2. Открыть noVNC в браузере
#    http://localhost:7900
#    Пароль: secret

# 3. Смотреть тесты в реальном времени!
```

### Порты

| Порт | Сервис | Описание |
|------|--------|----------|
| **7900** | noVNC | Браузер в браузере (VNC через HTTP) |
| **5900** | VNC | Для VNC-клиентов (Remmina, TigerVNC) |
| **4444** | Selenium | WebDriver API |
| **9001** | Reports | HTML-отчёты тестов |

### Режим HEADLESS vs VISIBLE

Управляется переменной `HEADLESS`:

| Значение | Режим | Когда использовать |
|----------|-------|-------------------|
| `HEADLESS=true` | Без GUI | CI/CD, быстрые прогоны |
| `HEADLESS=false` | С GUI | Отладка, демонстрация |

```yaml
# docker-compose.selenium.yml
environment:
  - HEADLESS=false  # ← Видимый режим для noVNC!
```

### Преимущества seleniarm

Используем `seleniarm/standalone-chromium` вместо `selenium/standalone-chrome`:

| Особенность | seleniarm | selenium |
|-------------|-----------|----------|
| ARM64 (Apple M1/M2/M3) | ✅ | ❌ |
| AMD64 (Intel/AMD) | ✅ | ✅ |
| Linux ARM | ✅ | ❌ |
| Chromium вместо Chrome | ✅ | Chrome |

---

## 📚 Глоссарий

| Термин | Определение |
|--------|-------------|
| **pytest** | Python-фреймворк для тестирования |
| **Fixture** | Функция подготовки данных/ресурсов для теста |
| **Selenium** | Инструмент автоматизации браузера |
| **WebDriver** | API управления браузером |
| **Selenium Grid** | Распределённый запуск браузеров |
| **Page Object** | Паттерн инкапсуляции UI |
| **Smoke test** | Базовая проверка работоспособности |
| **Parametrize** | Запуск одного теста с разными данными |
| **Headless** | Режим браузера без GUI |

---

## 🎯 Резюме

### Что используется и почему

| Технология | Почему |
|------------|--------|
| **pytest** | Простой синтаксис, fixtures, параметризация |
| **Selenium** | Тестирование реального UI в браузере |
| **Selenium Grid** | Изоляция, работа в Docker, CI/CD |
| **Page Object** | Поддерживаемость, DRY |
| **pytest-html** | Красивые отчёты для команды |

### Покрытие тестами

| Компонент | Тип теста |
|-----------|-----------|
| `/api/health` | Smoke |
| `/api/calc` операции | API + параметризация |
| Деление на ноль | API (edge case) |
| Nginx proxy | Интеграционный |
| UI калькулятора | Selenium |
| Десятичные/отрицательные | Selenium |
| Последовательные вычисления | Selenium |

---

**Документация создана автоматически на основе кодовой базы проекта.**


# 📚 Урок: Docker для Frontend + Backend (75 минут)

## Материалы для преподавателя

---

## 🎯 Цели урока

После урока студент умеет:
- Проверить установку Docker
- Собрать образы из Dockerfile
- Создать Docker-сеть для связи контейнеров
- Запустить контейнеры с пробросом портов
- Заходить внутрь контейнера (docker exec)
- Смотреть логи и отлаживать проблемы
- Использовать Docker Compose

**Бонус для QA:** запускать автотесты в Docker.

---

## 📋 Структура урока

| Время | Тема | Формат |
|-------|------|--------|
| 0-5 мин | Вступление, мотивация | Лекция |
| 5-12 мин | Проверка Docker | Практика |
| 12-18 мин | Термины: image, container, network | Лекция + схема |
| 18-25 мин | Клонирование и сборка образов | Практика |
| 25-30 мин | Создание Docker-сети | Практика |
| 30-37 мин | Запуск backend | Практика |
| 37-45 мин | Запуск frontend + исследование изнутри | Практика |
| 45-49 мин | Инструменты отладки | Демо |
| 49-53 мин | Остановка и очистка | Практика |
| 53-60 мин | Docker Compose | Практика |
| **60-75 мин** | **Бонус: Тесты для QA** | Практика |

---

## 🚀 Перед уроком (подготовка)

### 1. Проверить что Docker работает у всех студентов
```bash
docker --version
docker run --rm hello-world
```

### 2. Опционально: загрузить готовые образы на Docker Hub
```bash
# Собрать локально
docker build -t calc-backend:local ./backend
docker build -t calc-frontend:local ./frontend

# Тегировать для Docker Hub
docker tag calc-backend:local [your-dockerhub]/calc-backend:ready
docker tag calc-frontend:local [your-dockerhub]/calc-frontend:ready

# Загрузить
docker push [your-dockerhub]/calc-backend:ready
docker push [your-dockerhub]/calc-frontend:ready
```

Это страховка — если у студента сборка зависнет.

---

## 📖 Материалы урока

### Часть 0-5 мин: Вступление

**Сказать:**
> Сегодня мы не пишем код. Мы работаем как DevOps-инженер: берём готовое приложение и запускаем его в Docker.
> 
> У нас калькулятор из двух частей — backend на Java и frontend на React. Задача — собрать, связать, запустить.

**Вопросы аудитории:**
1. Кто уже использовал Docker?
2. Чем image отличается от container?
3. Что такое Dockerfile?

---

### Часть 5-12 мин: Проверка Docker

**Задание для студентов (на доске):**

```bash
docker --version
docker compose version
docker info | head -20
docker run --rm hello-world
```

**Чек-лист:**
- [ ] Версия Docker показалась
- [ ] Версия Compose показалась
- [ ] docker info не выдал ошибку
- [ ] hello-world напечатал приветствие

**Если ошибка "Cannot connect to Docker daemon":**
> Docker установлен, но служба не запущена. Запустите Docker Desktop.

---

### Часть 12-18 мин: Термины

**Нарисовать схему на доске:**

```
┌─────────────────────────────────────────────────────────────┐
│                    ТВОЙ КОМПЬЮТЕР (HOST)                    │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐   │
│   │              Docker Network: calc-net               │   │
│   │                                                     │   │
│   │   ┌─────────────────┐    ┌─────────────────┐       │   │
│   │   │  calc-backend   │    │  calc-frontend  │       │   │
│   │   │   Java:8080     │◄───│   nginx:80      │       │   │
│   │   └────────┬────────┘    └────────┬────────┘       │   │
│   │            │                      │                │   │
│   └────────────│──────────────────────│────────────────┘   │
│                │                      │                    │
│           -p 8080:8080           -p 3000:80                │
│                │                      │                    │
│           localhost:8080         localhost:3000            │
└─────────────────────────────────────────────────────────────┘
```

**Вопросы:**
- Зачем `-p 3000:80`? → Проброс порта
- Как контейнеры находят друг друга? → Docker DNS
- Почему frontend шлёт на /api, а не localhost:8080? → nginx прокси, нет CORS

---

### Часть 18-25 мин: Сборка образов

**Задание:**

```bash
# Клонировать
git clone https://github.com/[org]/calculator.git
cd calculator

# Собрать backend (3-5 мин)
docker build -t calc-backend:local ./backend

# Собрать frontend (2-3 мин)
docker build -t calc-frontend:local ./frontend

# Проверить
docker images | grep calc-
```

**Пока собирается, спросить:**
> Смотрите в терминал. Видите "Step 1/8, Step 2/8"? Что это?

Ответ: каждый шаг Dockerfile = слой образа.

**Страховка если долго:**
```bash
docker pull [dockerhub]/calc-backend:ready
docker tag [dockerhub]/calc-backend:ready calc-backend:local
```

---

### Часть 25-30 мин: Создание сети

**Задание:**
```bash
docker network create calc-net
docker network ls | grep calc-
```

**Вопрос:**
> Что будет если запустить контейнеры без общей сети?

Ответ: DNS не работает, контейнеры не найдут друг друга.

---

### Часть 30-37 мин: Запуск backend

**Задание:**
```bash
docker run -d --name calc-backend \
  --network calc-net \
  -p 8080:8080 \
  calc-backend:local

docker ps
docker logs -f calc-backend
```

**Тест API:**
```bash
curl -s -X POST http://localhost:8080/api/calc \
  -H "Content-Type: application/json" \
  -d '{"a":5,"b":3,"op":"*"}'
```

Ожидаемо: `{"result":15.0,...}`

**Вопросы:**
- Где работает Java — на хосте или в контейнере? → В контейнере
- Почему curl localhost работает? → Проброс порта -p

---

### Часть 37-45 мин: Запуск frontend

**Задание:**
```bash
docker run -d --name calc-frontend \
  --network calc-net \
  -p 3000:80 \
  calc-frontend:local

docker ps
```

**Открыть в браузере:** http://localhost:3000

**Зайти внутрь:**
```bash
docker exec -it calc-frontend sh

# Внутри:
cat /etc/nginx/conf.d/default.conf
ls -la /usr/share/nginx/html
exit
```

**Вопрос:**
> Что вы видите в nginx конфиге в location /api/?

Ответ: proxy_pass http://calc-backend:8080

---

### Часть 45-49 мин: Отладка

**Демо команд:**
```bash
docker port calc-frontend
docker port calc-backend
docker inspect calc-backend --format '{{json .NetworkSettings.Networks}}'
docker exec calc-backend env
```

---

### Часть 49-53 мин: Очистка

**Задание:**
```bash
docker stop calc-frontend calc-backend
docker rm calc-frontend calc-backend
docker network rm calc-net

docker ps -a | grep calc-
docker network ls | grep calc-
```

---

### Часть 53-60 мин: Docker Compose

**Задание:**
```bash
docker compose up --build -d
docker compose ps
docker compose logs -f  # Ctrl+C выйти
```

**Открыть:** http://localhost:3000

**Вопросы:**
- Почему не создавали сеть? → Compose создаёт автоматически
- Как frontend находит backend? → По имени сервиса "backend"

**Остановить:**
```bash
docker compose down
```

---

## 🧪 Бонус для QA (60-75 мин)

**Сказать:**
> Приложение работает. Как проверить что оно работает ПРАВИЛЬНО? Автотесты!

### API-тесты (без браузера)

```bash
docker compose -f docker-compose.test.yml up --build --abort-on-container-exit
```

**Ожидаемый вывод:**
```
✅ Backend готов
✅ Frontend готов
🚀 Все сервисы готовы!

test_api.py::TestSmoke::test_backend_health PASSED
test_api.py::TestSmoke::test_frontend_available PASSED
...
8 passed in 2.34s
```

### Selenium-тесты (с браузером)

```bash
docker compose -f docker-compose.selenium.yml up --build --abort-on-container-exit
```

**Показать Selenium Grid UI:** http://localhost:4444

**Отчёт:** `./test-reports/report.html`

---

## ❓ Финальные вопросы

| Вопрос | Ответ |
|--------|-------|
| Какая команда показывает запущенные контейнеры? | `docker ps` |
| Как посмотреть логи? | `docker logs -f <name>` |
| Как зайти внутрь контейнера? | `docker exec -it <name> sh` |
| Как собрать образ? | `docker build -t name:tag .` |
| Что делает docker compose up --build? | Собирает образы и запускает сервисы |

---

## 📝 Домашнее задание

1. Добавить операцию `%` (остаток от деления) в backend
2. Изменить порт frontend с 3000 на 8888
3. Добавить healthcheck для frontend в docker-compose.yml
4. Написать команду, которая покажет IP контейнера backend
